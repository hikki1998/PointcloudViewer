# Findings

## Session 2026-04-04
- 已读取 `PROJECT_CONTEXT.md`，确认当前项目核心是 Qt + OSG 的桌面点云查看器，工程文件已持久化多数据集和业务数据。
- 当前需求不是增加通用 GIS 全家桶，而是在现有工程/业务结构内，把 CRS 从局部业务入口提升为工程级属性。
- 仓库指引明确：工程文件结构变更后，需要检查旧工程兼容和新字段保存加载。
- 当前 CRS 只存在于 `RoutePlanningOptions::crs`，定义位于 `src/domain/InspectionRoutePlanning.h`，字段仅有 `sourceEpsg` 与 `targetEpsg`。
- `MainWindow` 在 `Inspection Route Planning` 组里仅暴露一个 `Source EPSG` 输入框，并在导入/导出 KML、KMZ 时检查它；这说明 CRS 目前被视为“航线互操作参数”，而不是工程级空间参考。
- 工程文件 `loadProjectFile/saveProjectFile` 目前保存 `routePlanning`，但没有独立 `projectProperties` / `coordinateSystems` 节点。
- `LasReader` 能从 LAS 头读取 `projectionText`（WKT / ASCII / GeoKey 摘要），但没有解析成标准 EPSG，也没有把 LAS 原始 CRS 变成工程属性建议值。
- `DataManager` 当前只管理点云、图片、轨迹，没有项目级元数据容器；如果要在工程树或详情页显示 CRS，需要新增工程元数据承载对象，或由 `MainWindow` 持有独立 `ProjectMetadata`。
- 工程树目前只有 `Point Clouds / Images / Trajectories` 三组，没有 project-root 节点，因此“工程属性”没有天然显示入口。
- `CMakeLists.txt` 已有可选 `LAS_VIEWER_ENABLE_PROJ` 和 `LAS_VIEWER_HAS_PROJ` 检测逻辑；代码里已通过 `proj_create_crs_to_crs` 做 EPSG 之间转换。
- 当前工作区 `3rd/` 下未发现 `proj` 目录，这意味着如果本地没有单独配置 `PROJ_ROOT`，CRS 转换能力很可能在当前构建中处于禁用状态。
- 用户后续要求直接配置独立 GDAL release，而不是继续走 `vcpkg` 安装。
- 已清理本轮 `vcpkg` 残留的 `buildtrees/gdal`、`buildtrees/proj` 以及缓存探测临时文件。
- 已部署 `GISInternals` 的 `MSVC 2019 x64 / GDAL 3.10.3` 运行时 release 到 `E:\code\thirdparty\gdal`。
- 已验证 `E:\code\thirdparty\gdal\bin\gdal\apps\gdalinfo.exe --version` 输出 `GDAL 3.10.3, released 2025/04/01`。
- 当前这套 GDAL 是运行时/工具包，包含 `bin`, `bin\\gdal-data`, `bin\\proj9`, `gdal` 工具链；不包含现成的 `include/lib` SDK 目录。
- 已新增 `src/domain/ProjectMetadata.h`，作为工程级 CRS 类型与 JSON 辅助函数的统一入口。
- `MainWindow` 现已持有 `ProjectCoordinateSystems`，保存工程时写入 `projectProperties.coordinateSystems`，加载旧工程时会从 `routePlanning.crs.sourceEpsg` 迁移为工程级 `pointCloudCrs`。
- 为了兼容旧工程文件，本轮仍保留 `routePlanning.crs` 的读取逻辑；运行时则由 `MainWindow` 内部把旧字段同步到工程级 `ProjectCoordinateSystems`。
- `routeSummary` 显示已切到工程级 CRS 摘要，而不是直接读取旧 `routePlanning.crs`。
- `src/crs/` 当前以主程序内模块形式编译，而不是独立 DLL；因此导出宏需要默认退化为空定义，不能直接按 `Q_DECL_IMPORT/Q_DECL_EXPORT` 的共享库模式处理。
- `CMakeLists.txt` 原先误引用了不存在的 `src/domain/ProjectMetadata.cpp`，同时没有把 `src/crs/` 源文件纳入主程序和 smoke test，导致新 CRS UI/转换代码虽已写出但不会进入构建。
- 航线导入导出现在已经直接接收 `ProjectCoordinateSystems`；旧 `routePlanningOptions_.crs` 仅保留为兼容旧工程 JSON 的过渡字段。
- 工程树现已加入 `Project -> Coordinate Systems` 入口，用户无需进入 `Inspection Route Planning` 面板即可发现并编辑工程级 CRS。
- 当前 Release 构建仍提示 `PROJ was enabled but not found`；这不会阻止主程序和 smoke test 通过，但坐标转换能力在未配置 `PROJ_ROOT` 的环境里仍是禁用状态。
- 本地 `E:\code\thirdparty\gdal` 确认只是运行时/工具包：有 `bin\proj_9.dll`，但没有 `include`、`lib`、`proj.h`、`proj.lib`，因此不能直接作为 `PROJ_ROOT` 提供给 CMake 编译链接。
- 已从 GISInternals 的 SDK 构建下载并落地 `release-1944-x64-dev.zip` 到 `E:\code\thirdparty\gdal_sdk_1944_x64`；该 SDK 已确认包含 `include\proj9\proj.h`、`lib\proj9.lib`、`bin\proj_9.dll`，同时也带有 `gdal.h` 与 `gdal_i.lib` 等开发文件。
- GISInternals SDK 的目录布局不是常见的 `include/proj.h + lib/proj.lib`，而是 `include/proj9/proj.h + lib/proj9.lib`；工程的 `CMakeLists.txt` 已扩展为兼容这种布局。
- 重新配置 `cmake -DPROJ_ROOT=E:/code/thirdparty/gdal_sdk_1944_x64` 后，CMake 已输出 `Using PROJ: E:/code/thirdparty/gdal_sdk_1944_x64/lib/proj9.lib`，说明工程级 CRS 转换链路现已真正启用。
- 按现有三方库的稳定目录约定，`E:\code\thirdparty\gdal` 现已切换为统一的 GDAL SDK/runtime 根目录，包含 `bin/`、`include/`、`lib/`、`.version`、`gdal-env.bat`、`SDKShell.bat`。
- 原先的纯运行时包已保留为备份目录 `E:\code\thirdparty\gdal_runtime_legacy_3103_20260404`，未直接删除。
- `CMakeLists.txt` 现已增加 `GDAL_ROOT` 支持，并在 `GDAL_ROOT` 存在且 `PROJ_ROOT` 为空或失效时，自动把 `PROJ_ROOT` 回落到 `GDAL_ROOT`。
- 重新配置时仅传 `-DGDAL_ROOT=E:/code/thirdparty/gdal -DPROJ_ROOT=` 即可重新命中稳定 GDAL 包，CMake 输出已变为 `Using GDAL SDK: ...` 与 `Using PROJ: E:/code/thirdparty/gdal/lib/proj9.lib`。
